# Changelog

## 1.2.0 - 2026-09-07

- Provider 初始化和会话断开增加模态进度条、阶段提示与全局防重复提交。
- 长操作期间暂停后台界面轮询，避免状态刷新与网络修改相互干扰。
- “接收方账号不存在”改为明确错误，提示对方先在同一控制服务器注册。
- 创建共享表单补充接收账号前置条件说明。

## 1.1.0 - 2026-09-07

- 将默认浏览器管理页升级为独立的 Microsoft Edge WebView2 桌面窗口。
- 关闭桌面窗口会同步停止本地 UI、心跳监控和应用进程。
- 所有 PowerShell/WireGuard 子进程使用 `CREATE_NO_WINDOW`，消除空白控制台闪现。
- 首页改为角色驱动的四步向导，只突出当前下一步；简化导航并增加 Provider/Consumer 三步流程条。
- Host 内嵌服务器地址锁定，减少误配置；未登录时禁用设备和网络操作。

## 1.0.0 - 2026-09-07

- 新增 Host、Provider、Client 三个一键启动 Windows GUI 程序。
- 重写中文响应式本地管理界面，覆盖设置、账号、设备、授权、连接、状态、诊断和日志。
- 支持本地嵌入式控制服务器，以及带 TLS 证书的独立云端控制服务器。
- 支持多用户、多 Provider 和同一 Provider 的多个并发 Consumer。
- Consumer 预览改为完全只读，不再创建短暂服务器 session。
- 修复 WireGuard 服务启动竞态、IPv6 防火墙地址兼容、异常虚拟网卡诊断和幂等恢复问题。
- Provider 防火墙按当前 Windows 网络配置文件创建，并为 Host 的 LocalSubnet 控制端口提供显式规则。
- 发布包说明文件改用 ASCII 文件名，避免 ZIP 中文文件名乱码。
